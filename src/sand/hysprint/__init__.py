# The experiment-info form JSON is stored as a WrittenNote at a fixed mainfile
# in the experiment upload; the note is found by that mainfile. The label is
# only its initial, user-editable text.
EXPERIMENT_INFO_LABEL = 'experiment_info'
EXPERIMENT_INFO_MAINFILE = 'experiment_info.archive.json'

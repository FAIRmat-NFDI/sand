# The experiment-info form JSON is stored as a WrittenNote with this label,
# at a fixed mainfile in the experiment upload.
EXPERIMENT_INFO_LABEL = 'experiment_info'
EXPERIMENT_INFO_MAINFILE = 'experiment_info.archive.json'
